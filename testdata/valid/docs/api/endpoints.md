# API Endpoints

This document describes the available API endpoints.

[Back to home](../index.md)

## GET /api/model

Returns the current model.

```json
{
  "version": "1.0",
  "elements": []
}
```
